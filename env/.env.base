# infra-base/env/.env.base
# Non-sensitive shared defaults — safe to commit

LOG_LEVEL=info
ENV=development

# Resource defaults
CPU_LIMIT=0.5
MEMORY_LIMIT=512M

# Health check port default
HEALTH_PORT=8080
