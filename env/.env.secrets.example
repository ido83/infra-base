# Copy this file to infra-base/env/.env.secrets and replace the placeholder values.
# Do not commit the real .env.secrets file.

DB_PASSWORD=replace-with-a-real-postgres-password
GRAFANA_PASSWORD=replace-with-a-real-grafana-password
