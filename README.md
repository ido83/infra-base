# infra-base

Shared Docker Compose building blocks for the app repos in this workspace.

Contents:

- `compose/` shared compose fragments
- `env/` shared non-secret env defaults
- `scripts/bootstrap-submodule.sh` helper for submodule-based setups

This repo should stay generic and reusable.
